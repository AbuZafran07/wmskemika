import React from 'react';
import ComingSoon from '../ComingSoon';

export default function AuditLog() {
  return <ComingSoon title="Audit Log" description="View complete system audit trail with before/after changes." />;
}
